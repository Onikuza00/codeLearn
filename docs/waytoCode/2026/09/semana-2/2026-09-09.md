# 2026-09-09 — Miércoles · Reorientación del plan { .section-fundamentos }

> Sesión corta, sin ejercicios. Día de replanteamiento y limpieza. Miércoles 09 · Semana 02 · Septiembre.

---

## 📅 Actividad del día

**Reorientación del aprendizaje hacia empleabilidad web genérica.** El plan pasa a tres frentes:

- **Symfony backend, activo:** avanzar y repasar lo aprendido con ejercicios prácticos escritos a mano, sin IA. El objetivo es el músculo de construir en frío, no correr a temario nuevo.
- **JS, consolidación:** DOM reforzado + `fetch` entero + `sessionStorage`/`localStorage` + módulos + los patrones 🔴 que reinciden.
- **React fuerte en octubre** (ya no aparcado), con el bloque JS cerrado antes.
- Curso de API de Claude, a horas residuales.

**Normas nuevas fijadas** (se comprueban SIEMPRE antes de dar un ejercicio):

1. **Fase de maquetación responsive en Tailwind al cierre de cada ejercicio**, sea del track que sea (JS, DOM, Symfony, backend). Se describe cómo debe verse, nunca las clases.
2. **Enunciados autocontenidos:** paso a paso, sin dar nada por sentado, sin tecnicismos sin definir, sin referencias a ejercicios anteriores.

**Limpieza:**

- "🚨 Repaso urgente" queda solo con **JS/DOM** y **Symfony**. Retiradas las chuletas del bloque de Arquitectura (guion, casos, importación masiva, frases).
- `docs/backend/01-php/01-metodos-basicos/` dividido en 5 subpáginas por tipo (cadenas, arrays, números, tipos y valores, depuración) — ahora son subsecciones en el menú. La sección `sort`/`rsort`/`usort`/`asort`/`ksort` ampliada con ejemplo y explicación por función.

**Herramientas:** instaladas dos skills de revisión de UI en `~/.claude/skills/` (global): `ss-review` (checklist) y `design-taste-frontend` (dirección/gusto). Para crítica y pulido de trabajo ya hecho, no para las reps de aprendizaje.

## ⏳ Pendiente para la próxima

- **Symfony `gestor-tareas`:** `ByDueDateSorter` (D6) está mal — es un copia-pega de `ByPrioritySorter` que ordena por prioridad, no por fecha. Reescribir con `usort()` + `<=>` sobre `getDueDate()`, decidiendo dónde caen las tareas sin fecha. Después: D5 (filtro Twig `fecha_relativa` — el servicio `DateHumanizer` ya está; falta la teoría del patrón Extension+Runtime en docs y luego el wiring), y bloque E (E2–E6 + migrar Tailwind al bundle).
- **JS:** cards de ejercicios de asincronía / módulos / almacenamiento (hay temario, no hay card).
- **Drilling DOM:** patrones 🔴 que reinciden — `setCustomValidity()`, los tres `contains()`, De Morgan, fuga global.
